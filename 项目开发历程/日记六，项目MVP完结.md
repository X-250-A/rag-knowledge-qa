## 今日完成：

- ##### 完成状态机接线流程

  - 实际体验下来，状态机无外乎：创建个专门的类持久化所有状态，在update类函数中更新状态，在编排各个功能时添加update类函数并添加状态的新值。

- ##### 完成前端撰写

  - 实际是模仿了travel项目的前端审美。有了模板，dsh完成页面的费用确实很低，不到一块钱就写完了。

- ##### 完成项目MVP，同时推送至github。

  - 不仅推送了，还学到了一些东西。

    - 在前后端完成后，我独自uvicorn进行用户的简单测试。还让我遇见了CORS跨域配置的问题。由于jwt中间件与CORS中间件添加的顺序不同（改前是jwt在下面），导致FastAPI先执行jwt鉴权导致前后端跨域配置失败，后续账号成功写入，而前端无法响应，导致登录流程卡住，后续同样账号密码只会提示failed to fetch或user already exist（对应登录 / 注册）。

    - 在修好bug后，github推动过程中，hermes将我误npm install的uvicorn同名污染包一并commit进入了github，找到了上次迭代的推送skill的漏洞并进行了修复。



## 笔者有感：

        今天完成了项目的MVP发布，让我没想到的是，前端的完成如此高效。看来以后找模板代码来参考最省token。不枉我第一个项目先ds裸写一遍，再让opencode加持的ds再优化一遍。上面也写了，花了一块钱不到（还是ds涨价后的价格）就完成了。不过长期这么整容易页面同质化造成审美疲劳，我或许可以让glm去github找前端项目参考源码。
