## （补档）今日成就：

- ##### parser解析器函数手写：
  
  - 手写pdf，md，docx三大类型文件的解析器函数，收获以下知识：认识了pathlib库中Path字符串拼接方法：“ str / str ”；认识了该库.suffix的获取Path对象后缀的方法；温习了with open打开文件夹的方法。
  
  - 对于解析器有了初步理解：解析器的目的是把各个类型文件解析出字符串。一般流程如下：打开文件 -> 提取文本内容 -> 返回字符串。对于pdf，还需要额外.close文件，避免文件打开占用句柄导致进程阻塞。对于md，直接with open读取文件即可

- ##### 完成documents路由函数撰写：
  
  - 首次接触磁盘文件相关操作，首次了解with open搭配await的写法（UploadFile对象使用.read()后，若不await，返回的是协程对象，并非真正的磁盘文件）
  
  - 对于删除逻辑的认识从仅删除数据库数据，拓展到同步删除数据库，向量库，磁盘文件三大数据。
  
  - 认识了metadata这一结构，可自动包裹所需数据传递，无需手动赋值。（具体的我还得回看源码）



## 笔者有感：

        为什么鸽了呢，因为今天在赶飞机，只能在现在把昨天的收获敲出来，可惜到现在，昨天的收获已经遗忘一部分了。项目还没有推荐，不过根据mimo的分析，剩下的步骤也没多少了。前端我打算让opencode全权负责了。上次消耗的时间有点久，现在ds涨价，我更得小心点。看看怎么减少token消耗。就这样吧。补上日期：



#### 2026.8.22




