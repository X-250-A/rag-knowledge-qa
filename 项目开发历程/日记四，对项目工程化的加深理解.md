## 昨日完成：

- ##### 完成document和chat路由函数

  -  这里只是初版函数，尤其是chat，仅进行非流式输出，SSE流式输出以及会话上下文管理还在补齐。



## 今日完成：

- ##### 完成部分文件的init注册

  - 今天总算了解到init的作用了：作为统一一个py文件夹中所有文件导出的出口。简单说，就是在__init__.py中导入该文件夹下所有类，加入__all__列表，后续跨文件导包就不需要繁琐地从根目录写绝对路径一个个导入。

    - 实际项目中的注册，根目录级别的init仅注册app单一入口

  - 以后这种事就交给AI就行。

- ##### 完成仿travel-assistant项目中提取agent层的决策，并完成ConversationManager会话状态机的撰写。

  - 今天对项目工程化拆分有了更深刻的理解。CRUD操作层，数据库配置层，单一领域服务层，多服务编排层，业务综合层（agent），逻辑分明，业务分明，方便职责划分与错误回溯和追责。我今天还企图把会话管理的逻辑塞入agent主入口，受教了。

  - 也手写了一遍ConversationManager会话状态机。我起初想直接把CRUD的conversation去掉，后面发现不对，这样做职责混乱，还会导致功能缺失。



## 笔者有感：

        还是得做项目，做项目了解业务逻辑是对的。

        至于为什么包含两天的进度，只是因为昨天鸽了。



### 2026.8.24
